server:
  port: 8000

spring:
  application:
    name: chat-gateway

logging:
  level:
    com.ai.chat: DEBUG
    org.springframework.cloud.gateway: DEBUG

# -------------------------------------------------------
# 以下配置已迁移到 Nacos，不再在本地维护：
#   spring.cloud.gateway.routes  -> Nacos: chat-gateway.yaml
#   jwt.secret                   -> Nacos: chat-gateway.yaml
#   auth.white-list              -> Nacos: chat-gateway.yaml
# -------------------------------------------------------
